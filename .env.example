# Example Environment Configuration
# Copy this file to .env and fill in the values. Do not commit .env to version control.

# Application Logging Level (INFO, DEBUG, WARNING, ERROR)
LOG_LEVEL=INFO

# Dataset Directory (Optional Override)
# DATA_DIR=/path/to/datasets

# Output Directory (Optional Override)
# RESULTS_DIR=/path/to/results
